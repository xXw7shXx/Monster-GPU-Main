# w7sh gamebot clean export

Fresh-history gamebot source export. Runtime databases, logs, caches, backups, and secrets are intentionally excluded.

Use `.env.example` for variable names only and provide secret values through runtime secret storage.
